"""
nexus-similarity-search-api — AutoGen Tool Wrappers
Generated by NEXUS v6.0. Talks real MCP protocol via _mcp_client.py.

Usage:
    from sdk_wrappers.autogen_tool import nexus_autogen_register
    nexus_autogen_register(agent)
"""

from typing import Annotated, Any

from ._mcp_client import call_mcp_tool

def rank_by_nmi_cosine_fusion(query: Annotated[str, 'JSON-serialized object representing the query item. Keys are feature names; values are strings (categorical/text) or numbers (numeric). Must share at least one key with corpus items.'], corpus: Annotated[list[str], 'Array of JSON-serialized objects, each representing a corpus item with the same feature schema as the query. Minimum 2 items required. Items with zero overlapping keys with the query are scored 0.'], nmi_weight: Annotated[float, 'Weight assigned to the NMI component in the fused score (0.0 to 1.0). Cosine weight is derived as 1 - nmi_weight. Set closer to 1.0 when categorical features dominate; closer to 0.0 for mostly numeric/text corpora.'] = None, top_k: Annotated[float, 'Number of top-ranked results to return. Must be between 1 and the corpus size. Results beyond top_k are discarded before returning.'] = None, numeric_bins: Annotated[float, 'Number of equal-width bins used to discretize numeric features before NMI computation. Higher values capture finer distinctions but require larger corpora to be statistically stable. Ignored when there are no numeric features.'] = None) -> Any:
    """Ranks a corpus of mixed-type items (text, categorical, numeric) against a query item using a unified score that fuses Normalized Mutual Information (for categorical/discrete feature dependencies) with cosine similarity (for continuous/text features). Use when your dataset has heterogeneous feature types and you need a single ranked list without precomputed embeddings or a persistent index. Do NOT use when all features are purely continuous/dense vectors (use a pure ANN index instead), when corpus exceeds 10 000 items per call (latency will degrade), or when you need approximate results at scale."""
    return call_mcp_tool("nexus_similarity_search_api_rank_by_nmi_cosine_fusion", {"query": query, "corpus": corpus, "nmi_weight": nmi_weight, "top_k": top_k, "numeric_bins": numeric_bins})

def score_nmi_cosine_pair(item_a: Annotated[str, 'JSON-serialized object for the first item. Keys are feature names; values are strings or numbers.'], item_b: Annotated[str, 'JSON-serialized object for the second item. Must share at least one feature key with item_a to produce a non-zero score.'], nmi_weight: Annotated[float, 'Weight of the NMI component in the fused score. Cosine weight = 1 - nmi_weight. Must match the weight used in ranking calls if this is used for threshold calibration.'] = None, numeric_bins: Annotated[float, 'Bin count for numeric discretization, consistent with what would be used in corpus ranking. Default 10.'] = None) -> Any:
    """Computes the fused NMI+cosine similarity score between exactly two items and returns the decomposed components (nmi_score, cosine_score, fused_score) alongside per-feature contributions. Use for explainability, threshold calibration, or validating why two specific items are ranked close or far apart. Do NOT use as a substitute for rank_by_nmi_cosine_fusion when ranking a corpus — calling this in a loop over N items is O(N) round-trips and will be slower and costlier than a single ranking call."""
    return call_mcp_tool("nexus_similarity_search_api_score_nmi_cosine_pair", {"item_a": item_a, "item_b": item_b, "nmi_weight": nmi_weight, "numeric_bins": numeric_bins})

def detect_dominant_feature_type(corpus_sample: Annotated[list[str], 'Array of JSON-serialized items (a representative sample, not necessarily the full corpus). Minimum 10 items recommended for stable type inference. Maximum 500 items accepted in a single detection call.'], cardinality_threshold: Annotated[float, 'Maximum number of distinct values a numeric feature may have before it is reclassified as categorical for NMI purposes. Features with unique-value count below this threshold are treated as categorical.'] = None) -> Any:
    """Analyzes a corpus payload and returns a breakdown of feature types (categorical, numeric, text) with their relative dominance and the recommended nmi_weight for optimal fusion. Use this as a calibration step before the first ranking call when you are unsure what nmi_weight to use. Do NOT use on every call in a hot path — run once per dataset schema and cache the recommended weight; schema does not change per query."""
    return call_mcp_tool("nexus_similarity_search_api_detect_dominant_feature_type", {"corpus_sample": corpus_sample, "cardinality_threshold": cardinality_threshold})

def batch_rank_nmi_cosine_multi_query(queries: Annotated[list[str], 'Array of JSON-serialized query objects. Each query is ranked independently against the shared corpus. Maximum 50 queries per batch call.'], corpus: Annotated[list[str], 'Shared corpus of JSON-serialized items ranked against every query. Minimum 2 items, maximum 5 000 items when used in batch mode (lower than single-query mode due to per-request memory constraints).'], nmi_weight: Annotated[float, 'Shared NMI weight applied uniformly across all queries in the batch. Per-query weight overrides are not supported in batch mode to keep the response schema deterministic.'] = None, top_k: Annotated[float, 'Number of top results returned per query. Applied uniformly to all queries in the batch.'] = None, numeric_bins: Annotated[float, 'Bin count for numeric discretization, applied uniformly across all queries.'] = None) -> Any:
    """Executes multiple independent similarity ranking operations in a single HTTP call, each with its own query item and shared corpus. Returns one ranked list per query. Use when you need to rank the same corpus against several query items simultaneously (e.g., recommendation for a batch of users, or multi-anchor nearest-neighbor search). Do NOT use when queries share no features with the corpus, when corpus differs per query (submit separate rank calls instead), or when batch size exceeds 50 queries (memory pressure per request)."""
    return call_mcp_tool("nexus_similarity_search_api_batch_rank_nmi_cosine_multi_query", {"queries": queries, "corpus": corpus, "nmi_weight": nmi_weight, "top_k": top_k, "numeric_bins": numeric_bins})

def explain_nmi_contribution_per_feature(query: Annotated[str, 'JSON-serialized query item used in the original ranking call.'], result_item: Annotated[str, 'JSON-serialized corpus item whose score you want explained. Should be one of the items returned by a prior rank_by_nmi_cosine_fusion call.'], nmi_weight: Annotated[float, 'Must match the nmi_weight used in the ranking call that produced this result, otherwise the decomposed score will not match the original fused score.'] = None, numeric_bins: Annotated[float, 'Must match the numeric_bins value used in the original ranking call for the NMI decomposition to be consistent.'] = None) -> Any:
    """Given a query and a single ranked result item, decomposes the fused score into per-feature NMI and cosine contributions, identifying which features drove similarity and which suppressed it. Use for debugging unexpected rankings, for building explainability layers in downstream products, or for feature importance analysis. Do NOT use as part of a ranking pipeline — it operates on a single (query, result) pair and does not scale to corpus-level ranking; it is a diagnostic tool only."""
    return call_mcp_tool("nexus_similarity_search_api_explain_nmi_contribution_per_feature", {"query": query, "result_item": result_item, "nmi_weight": nmi_weight, "numeric_bins": numeric_bins})


def nexus_autogen_register(agent) -> None:
    """Register all nexus-similarity-search-api tools on an AutoGen agent."""
    agent.register_for_llm(name="nexus_similarity_search_api_rank_by_nmi_cosine_fusion", description='Ranks a corpus of mixed-type items (text, categorical, numeric) against a query item using a unified score that fuses Normalized Mutual Information (for categorical/discrete feature dependencies) with cosine similarity (for continuous/text features). Use when your dataset has heterogeneous feature types and you need a single ranked list without precomputed embeddings or a persistent index. Do NOT use when all features are purely continuous/dense vectors (use a pure ANN index instead), when corpus exceeds 10 000 items per call (latency will degrade), or when you need approximate results at scale.')(rank_by_nmi_cosine_fusion)
    agent.register_for_execution(name="nexus_similarity_search_api_rank_by_nmi_cosine_fusion")(rank_by_nmi_cosine_fusion)
    agent.register_for_llm(name="nexus_similarity_search_api_score_nmi_cosine_pair", description='Computes the fused NMI+cosine similarity score between exactly two items and returns the decomposed components (nmi_score, cosine_score, fused_score) alongside per-feature contributions. Use for explainability, threshold calibration, or validating why two specific items are ranked close or far apart. Do NOT use as a substitute for rank_by_nmi_cosine_fusion when ranking a corpus — calling this in a loop over N items is O(N) round-trips and will be slower and costlier than a single ranking call.')(score_nmi_cosine_pair)
    agent.register_for_execution(name="nexus_similarity_search_api_score_nmi_cosine_pair")(score_nmi_cosine_pair)
    agent.register_for_llm(name="nexus_similarity_search_api_detect_dominant_feature_type", description='Analyzes a corpus payload and returns a breakdown of feature types (categorical, numeric, text) with their relative dominance and the recommended nmi_weight for optimal fusion. Use this as a calibration step before the first ranking call when you are unsure what nmi_weight to use. Do NOT use on every call in a hot path — run once per dataset schema and cache the recommended weight; schema does not change per query.')(detect_dominant_feature_type)
    agent.register_for_execution(name="nexus_similarity_search_api_detect_dominant_feature_type")(detect_dominant_feature_type)
    agent.register_for_llm(name="nexus_similarity_search_api_batch_rank_nmi_cosine_multi_query", description='Executes multiple independent similarity ranking operations in a single HTTP call, each with its own query item and shared corpus. Returns one ranked list per query. Use when you need to rank the same corpus against several query items simultaneously (e.g., recommendation for a batch of users, or multi-anchor nearest-neighbor search). Do NOT use when queries share no features with the corpus, when corpus differs per query (submit separate rank calls instead), or when batch size exceeds 50 queries (memory pressure per request).')(batch_rank_nmi_cosine_multi_query)
    agent.register_for_execution(name="nexus_similarity_search_api_batch_rank_nmi_cosine_multi_query")(batch_rank_nmi_cosine_multi_query)
    agent.register_for_llm(name="nexus_similarity_search_api_explain_nmi_contribution_per_feature", description='Given a query and a single ranked result item, decomposes the fused score into per-feature NMI and cosine contributions, identifying which features drove similarity and which suppressed it. Use for debugging unexpected rankings, for building explainability layers in downstream products, or for feature importance analysis. Do NOT use as part of a ranking pipeline — it operates on a single (query, result) pair and does not scale to corpus-level ranking; it is a diagnostic tool only.')(explain_nmi_contribution_per_feature)
    agent.register_for_execution(name="nexus_similarity_search_api_explain_nmi_contribution_per_feature")(explain_nmi_contribution_per_feature)