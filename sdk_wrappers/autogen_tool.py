"""
nexus-similarity-search-api — AutoGen Tool Wrappers
Generated by NEXUS v6.0. Talks real MCP protocol via _mcp_client.py.

Usage:
    from sdk_wrappers.autogen_tool import nexus_autogen_register
    nexus_autogen_register(agent)
"""

from typing import Annotated, Any

from ._mcp_client import call_mcp_tool

def rank_by_nmi_cosine_hybrid(query: Annotated[str, 'Raw query item: plain text, a comma-separated categorical sequence, or a JSON-encoded integer array representing a discrete time series. Must be non-empty and consistent in type with corpus items.'], corpus: Annotated[list[str], 'Array of raw corpus items to rank. Each item must be the same type as query. Minimum 2 items required to compute marginal entropy weighting. Maximum 10,000 items per call.'], data_modality: Annotated[str, "Declares the semantic type of query and corpus items for tokenization and joint distribution estimation. One of: 'text' (whitespace-tokenized), 'categorical' (comma-separated discrete labels), 'discrete_timeseries' (JSON int array, binned by Sturges rule). Required — do not guess."], top_k: Annotated[float, 'Number of top-ranked results to return, sorted descending by hybrid score. Must be between 1 and min(corpus length, 500).'] = None, nmi_weight_override: Annotated[float, 'Fixed weight for the NMI component in [0.0, 1.0]; cosine weight becomes 1 - nmi_weight_override. If null, weight is computed adaptively from corpus marginal entropy: high-entropy corpora increase NMI weight. Provide a value only when you have a domain-specific reason to override entropy-based calibration.'] = None) -> Any:
    """Ranks a corpus of raw items (text, discrete categories, or integer time series) against a query using a weighted hybrid of Normalized Mutual Information and cosine similarity, where weights are determined adaptively by the marginal entropy of the corpus. Use when your data has non-linear statistical dependencies that cosine alone would miss, or when working with categorical/discrete distributions without a prebuilt embedding index. Do NOT use for continuous high-dimensional float vectors already embedded — cosine-only is faster and sufficient in that case; do NOT use if corpus exceeds 10,000 items per call (use batch endpoint instead)."""
    return call_mcp_tool("nexus_similarity_search_api_rank_by_nmi_cosine_hybrid", {"query": query, "corpus": corpus, "data_modality": data_modality, "top_k": top_k, "nmi_weight_override": nmi_weight_override})

def compute_pairwise_nmi_matrix(items: Annotated[list[str], 'Array of raw items for which to compute pairwise NMI. All items must share the same modality. Minimum 2, maximum 500.'], data_modality: Annotated[str, "Tokenization and binning strategy: 'text', 'categorical', or 'discrete_timeseries'. Must match the actual format of items."], return_marginal_entropies: Annotated[bool, 'If true, includes per-item marginal entropy H(X_i) in the response alongside the NMI matrix. Useful for identifying low-entropy items that may distort similarity scores.'] = None) -> Any:
    """Computes the full N x N Normalized Mutual Information matrix for a set of raw items, returning both the matrix and per-item marginal entropy values. Use for clustering preparation, feature selection diagnostics, or any downstream task that needs the full dependency structure of a corpus rather than a ranked list against a single query. Do NOT use as a ranking primitive — this is O(N^2) and intended for offline analysis, not per-request retrieval. Do NOT call with more than 500 items; use the batch endpoint for larger corpora."""
    return call_mcp_tool("nexus_similarity_search_api_compute_pairwise_nmi_matrix", {"items": items, "data_modality": data_modality, "return_marginal_entropies": return_marginal_entropies})

def estimate_corpus_entropy_profile(corpus: Annotated[list[str], 'Raw corpus items to profile. Same format constraints as rank_by_nmi_cosine_hybrid. Minimum 2, maximum 10,000.'], data_modality: Annotated[str, "Tokenization strategy: 'text', 'categorical', or 'discrete_timeseries'."]) -> Any:
    """Computes the marginal entropy of each item in a corpus and the joint entropy distribution across the full set, returning the entropy-based NMI weight that rank_by_nmi_cosine_hybrid would apply automatically. Use to audit or preview the adaptive weighting before committing to a ranking call, or to detect degenerate corpora where all items have near-zero entropy (making NMI unreliable). Do NOT use as a substitute for ranking — this endpoint only returns entropy diagnostics, not similarity scores."""
    return call_mcp_tool("nexus_similarity_search_api_estimate_corpus_entropy_profile", {"corpus": corpus, "data_modality": data_modality})

def score_candidate_pair_nmi_cosine(item_a: Annotated[str, 'First raw item of the pair. Non-empty, same modality as item_b.'], item_b: Annotated[str, 'Second raw item of the pair. Non-empty, same modality as item_a.'], data_modality: Annotated[str, "Tokenization strategy: 'text', 'categorical', or 'discrete_timeseries'."], nmi_weight_override: Annotated[float, 'Fixed NMI weight in [0.0, 1.0]. If null, inferred from pair-level joint entropy (less accurate than corpus-level; prefer explicit override for isolated pair scoring).'] = None) -> Any:
    """Computes the NMI score, cosine score, and hybrid weighted score for exactly one (query, candidate) pair without needing a full corpus. Entropy-based weight is estimated from the pair's joint distribution alone (no corpus marginal). Use for spot-checks, unit-level debugging of scoring logic, or integration tests where you need a deterministic score for a known pair. Do NOT use in production ranking loops — call rank_by_nmi_cosine_hybrid instead, which benefits from corpus-level entropy calibration that this endpoint cannot provide for isolated pairs."""
    return call_mcp_tool("nexus_similarity_search_api_score_candidate_pair_nmi_cosine", {"item_a": item_a, "item_b": item_b, "data_modality": data_modality, "nmi_weight_override": nmi_weight_override})

def batch_rank_multiquery_nmi_cosine(queries: Annotated[list[str], 'Array of raw query items, all of the same modality. Minimum 2, maximum 50 queries per batch call.'], corpus: Annotated[list[str], 'Shared corpus to rank against all queries. Minimum 2, maximum 10,000 items.'], data_modality: Annotated[str, "Tokenization strategy applied uniformly to all queries and corpus items: 'text', 'categorical', or 'discrete_timeseries'."], top_k: Annotated[float, 'Number of top-ranked corpus items returned per query. Applied uniformly across all queries. Between 1 and min(corpus length, 500).'] = None, nmi_weight_override: Annotated[float, 'Fixed NMI weight in [0.0, 1.0] applied to all queries. If null, corpus marginal entropy determines the weight once and reuses it across all queries in the batch.'] = None) -> Any:
    """Executes NMI-Cosine hybrid ranking for multiple queries against a shared corpus in a single request, sharing corpus entropy computation across all queries to reduce total cost. Use when you need to rank the same corpus against 2 or more queries simultaneously (e.g., multi-faceted retrieval, ensemble query expansion). Do NOT use for a single query — rank_by_nmi_cosine_hybrid is cheaper. Do NOT use when each query has a different corpus — this endpoint assumes one shared corpus across all queries."""
    return call_mcp_tool("nexus_similarity_search_api_batch_rank_multiquery_nmi_cosine", {"queries": queries, "corpus": corpus, "data_modality": data_modality, "top_k": top_k, "nmi_weight_override": nmi_weight_override})


def nexus_autogen_register(agent) -> None:
    """Register all nexus-similarity-search-api tools on an AutoGen agent."""
    agent.register_for_llm(name="nexus_similarity_search_api_rank_by_nmi_cosine_hybrid", description='Ranks a corpus of raw items (text, discrete categories, or integer time series) against a query using a weighted hybrid of Normalized Mutual Information and cosine similarity, where weights are determined adaptively by the marginal entropy of the corpus. Use when your data has non-linear statistical dependencies that cosine alone would miss, or when working with categorical/discrete distributions without a prebuilt embedding index. Do NOT use for continuous high-dimensional float vectors already embedded — cosine-only is faster and sufficient in that case; do NOT use if corpus exceeds 10,000 items per call (use batch endpoint instead).')(rank_by_nmi_cosine_hybrid)
    agent.register_for_execution(name="nexus_similarity_search_api_rank_by_nmi_cosine_hybrid")(rank_by_nmi_cosine_hybrid)
    agent.register_for_llm(name="nexus_similarity_search_api_compute_pairwise_nmi_matrix", description='Computes the full N x N Normalized Mutual Information matrix for a set of raw items, returning both the matrix and per-item marginal entropy values. Use for clustering preparation, feature selection diagnostics, or any downstream task that needs the full dependency structure of a corpus rather than a ranked list against a single query. Do NOT use as a ranking primitive — this is O(N^2) and intended for offline analysis, not per-request retrieval. Do NOT call with more than 500 items; use the batch endpoint for larger corpora.')(compute_pairwise_nmi_matrix)
    agent.register_for_execution(name="nexus_similarity_search_api_compute_pairwise_nmi_matrix")(compute_pairwise_nmi_matrix)
    agent.register_for_llm(name="nexus_similarity_search_api_estimate_corpus_entropy_profile", description='Computes the marginal entropy of each item in a corpus and the joint entropy distribution across the full set, returning the entropy-based NMI weight that rank_by_nmi_cosine_hybrid would apply automatically. Use to audit or preview the adaptive weighting before committing to a ranking call, or to detect degenerate corpora where all items have near-zero entropy (making NMI unreliable). Do NOT use as a substitute for ranking — this endpoint only returns entropy diagnostics, not similarity scores.')(estimate_corpus_entropy_profile)
    agent.register_for_execution(name="nexus_similarity_search_api_estimate_corpus_entropy_profile")(estimate_corpus_entropy_profile)
    agent.register_for_llm(name="nexus_similarity_search_api_score_candidate_pair_nmi_cosine", description="Computes the NMI score, cosine score, and hybrid weighted score for exactly one (query, candidate) pair without needing a full corpus. Entropy-based weight is estimated from the pair's joint distribution alone (no corpus marginal). Use for spot-checks, unit-level debugging of scoring logic, or integration tests where you need a deterministic score for a known pair. Do NOT use in production ranking loops — call rank_by_nmi_cosine_hybrid instead, which benefits from corpus-level entropy calibration that this endpoint cannot provide for isolated pairs.")(score_candidate_pair_nmi_cosine)
    agent.register_for_execution(name="nexus_similarity_search_api_score_candidate_pair_nmi_cosine")(score_candidate_pair_nmi_cosine)
    agent.register_for_llm(name="nexus_similarity_search_api_batch_rank_multiquery_nmi_cosine", description='Executes NMI-Cosine hybrid ranking for multiple queries against a shared corpus in a single request, sharing corpus entropy computation across all queries to reduce total cost. Use when you need to rank the same corpus against 2 or more queries simultaneously (e.g., multi-faceted retrieval, ensemble query expansion). Do NOT use for a single query — rank_by_nmi_cosine_hybrid is cheaper. Do NOT use when each query has a different corpus — this endpoint assumes one shared corpus across all queries.')(batch_rank_multiquery_nmi_cosine)
    agent.register_for_execution(name="nexus_similarity_search_api_batch_rank_multiquery_nmi_cosine")(batch_rank_multiquery_nmi_cosine)